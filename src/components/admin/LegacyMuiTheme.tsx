import React from 'react';

/**
 * Migration scaffolding completed — all @mui dependencies removed.
 */
export default function LegacyMuiTheme({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
